# Environment Configuration

# Development
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

# API URLs (replace with actual endpoints)
DEV_API_URL=https://api-dev.example.com
RC_API_URL=https://api-rc.example.com
STAGE_API_URL=https://api-stage.example.com
PROD_API_URL=https://api.example.com

# Database URLs (use environment-specific secrets in GitHub)
# DEV_DATABASE_URL=postgresql://user:pass@dev-db.example.com/db
# RC_DATABASE_URL=postgresql://user:pass@rc-db.example.com/db
# STAGE_DATABASE_URL=postgresql://user:pass@stage-db.example.com/db
# PROD_DATABASE_URL=postgresql://user:pass@prod-db.example.com/db
